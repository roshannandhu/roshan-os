export type VideoState =
  "idle" | "connecting" | "connected" | "reconnecting" | "exhausted" | "hidden";

export type AudioState =
  "idle" | "connecting" | "connected" | "reconnecting" | "exhausted" | "hidden" | "blocked";
